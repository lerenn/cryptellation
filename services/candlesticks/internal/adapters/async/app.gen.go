// Package "async" provides primitives to interact with the AsyncAPI specification.
//
// Code generated by github.com/lerenn/asyncapi-codegen version v0.5.0 DO NOT EDIT.
package async

import (
	"fmt"
)

// AppSubscriber represents all handlers that are expecting messages for App
type AppSubscriber interface {
	// CandlesticksListRequest
	CandlesticksListRequest(msg CandlesticksListRequestMessage)
}

// AppController is the structure that provides publishing capabilities to the
// developer and and connect the broker with the App
type AppController struct {
	brokerController BrokerController
	stopSubscribers  map[string]chan interface{}
	errChan          chan Error
}

// NewAppController links the App to the broker
func NewAppController(bs BrokerController) (*AppController, error) {
	if bs == nil {
		return nil, ErrNilBrokerController
	}

	return &AppController{
		brokerController: bs,
		stopSubscribers:  make(map[string]chan interface{}),
		errChan:          make(chan Error, 256),
	}, nil
}

// Errors will give back the channel that contains errors and that you can listen to handle errors
// Please take a look at Error struct form information on error
func (c AppController) Errors() <-chan Error {
	return c.errChan
}

// Close will clean up any existing resources on the controller
func (c *AppController) Close() {
	c.UnsubscribeAll()
	close(c.errChan)
}

// SubscribeAll will subscribe to channels on which the app is expecting messages
func (c *AppController) SubscribeAll(as AppSubscriber) error {
	if as == nil {
		return ErrNilAppSubscriber
	}

	if err := c.SubscribeCandlesticksListRequest(as.CandlesticksListRequest); err != nil {
		return err
	}

	return nil
}

// UnsubscribeAll will unsubscribe all remaining subscribed channels
func (c *AppController) UnsubscribeAll() {
	c.UnsubscribeCandlesticksListRequest()
}

// SubscribeCandlesticksListRequest will subscribe to new messages from 'candlesticks.list.request' channel
func (c *AppController) SubscribeCandlesticksListRequest(fn func(msg CandlesticksListRequestMessage)) error {
	// Check if there is already a subscription
	_, exists := c.stopSubscribers["candlesticks.list.request"]
	if exists {
		return fmt.Errorf("%w: candlesticks.list.request channel is already subscribed", ErrAlreadySubscribedChannel)
	}

	// Subscribe to broker channel
	msgs, stop, err := c.brokerController.Subscribe("candlesticks.list.request")
	if err != nil {
		return err
	}

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for um, open := <-msgs; open; um, open = <-msgs {
			msg, err := newCandlesticksListRequestMessageFromUniversalMessage(um)
			if err != nil {
				c.errChan <- Error{
					Channel: "candlesticks.list.request",
					Err:     err,
				}
			} else {
				fn(msg)
			}
		}
	}()

	// Add the stop channel to the inside map
	c.stopSubscribers["candlesticks.list.request"] = stop

	return nil
}

// UnsubscribeCandlesticksListRequest will unsubscribe messages from 'candlesticks.list.request' channel
func (c *AppController) UnsubscribeCandlesticksListRequest() {
	stopChan, exists := c.stopSubscribers["candlesticks.list.request"]
	if !exists {
		return
	}

	stopChan <- true
	delete(c.stopSubscribers, "candlesticks.list.request")
}

// PublishCandlesticksListResponse will publish messages to 'candlesticks.list.response' channel
func (c *AppController) PublishCandlesticksListResponse(msg CandlesticksListResponseMessage) error {
	// Convert to UniversalMessage
	um, err := msg.toUniversalMessage()
	if err != nil {
		return err
	}

	// Publish on event broker
	return c.brokerController.Publish("candlesticks.list.response", um)
}

// Listen will let the controller handle subscriptions and will be interrupted
// only when an struct is sent on the stop channel
func (c *AppController) Listen(stop <-chan interface{}) {
	<-stop
}
