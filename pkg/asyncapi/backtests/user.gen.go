// Package "backtests" provides primitives to interact with the AsyncAPI specification.
//
// Code generated by github.com/lerenn/asyncapi-codegen version v0.26.0 DO NOT EDIT.
package backtests

import (
	"context"
	"fmt"

	"github.com/lerenn/asyncapi-codegen/pkg/extensions"

	"github.com/google/uuid"
)

// UserSubscriber represents all handlers that are expecting messages for User
type UserSubscriber interface {
	// CryptellationBacktestsAccountsListResponse subscribes to messages placed on the 'cryptellation.backtests.accounts.list.response' channel
	CryptellationBacktestsAccountsListResponse(ctx context.Context, msg BacktestsAccountsListResponseMessage)

	// CryptellationBacktestsAdvanceResponse subscribes to messages placed on the 'cryptellation.backtests.advance.response' channel
	CryptellationBacktestsAdvanceResponse(ctx context.Context, msg BacktestsAdvanceResponseMessage)

	// CryptellationBacktestsCreateResponse subscribes to messages placed on the 'cryptellation.backtests.create.response' channel
	CryptellationBacktestsCreateResponse(ctx context.Context, msg BacktestsCreateResponseMessage)

	// CryptellationBacktestsEventsID subscribes to messages placed on the 'cryptellation.backtests.events.{id}' channel
	CryptellationBacktestsEventsID(ctx context.Context, msg BacktestsEventMessage)

	// CryptellationBacktestsOrdersCreateResponse subscribes to messages placed on the 'cryptellation.backtests.orders.create.response' channel
	CryptellationBacktestsOrdersCreateResponse(ctx context.Context, msg BacktestsOrdersCreateResponseMessage)

	// CryptellationBacktestsOrdersListResponse subscribes to messages placed on the 'cryptellation.backtests.orders.list.response' channel
	CryptellationBacktestsOrdersListResponse(ctx context.Context, msg BacktestsOrdersListResponseMessage)

	// CryptellationBacktestsSubscribeResponse subscribes to messages placed on the 'cryptellation.backtests.subscribe.response' channel
	CryptellationBacktestsSubscribeResponse(ctx context.Context, msg BacktestsSubscribeResponseMessage)
}

// UserController is the structure that provides publishing capabilities to the
// developer and and connect the broker with the User
type UserController struct {
	controller
}

// NewUserController links the User to the broker
func NewUserController(bc extensions.BrokerController, options ...ControllerOption) (*UserController, error) {
	// Check if broker controller has been provided
	if bc == nil {
		return nil, extensions.ErrNilBrokerController
	}

	// Create default controller
	controller := controller{
		broker:        bc,
		subscriptions: make(map[string]extensions.BrokerChannelSubscription),
		logger:        extensions.DummyLogger{},
		middlewares:   make([]extensions.Middleware, 0),
	}

	// Apply options
	for _, option := range options {
		option(&controller)
	}

	return &UserController{controller: controller}, nil
}

func (c UserController) wrapMiddlewares(
	middlewares []extensions.Middleware,
	callback extensions.NextMiddleware,
) func(ctx context.Context, msg *extensions.BrokerMessage) error {
	var called bool

	// If there is no more middleware
	if len(middlewares) == 0 {
		return func(ctx context.Context, msg *extensions.BrokerMessage) error {
			// Call the callback if it exists and it has not been called already
			if callback != nil && !called {
				called = true
				return callback(ctx)
			}

			// Nil can be returned, as the callback has already been called
			return nil
		}
	}

	// Get the next function to call from next middlewares or callback
	next := c.wrapMiddlewares(middlewares[1:], callback)

	// Wrap middleware into a check function that will call execute the middleware
	// and call the next wrapped middleware if the returned function has not been
	// called already
	return func(ctx context.Context, msg *extensions.BrokerMessage) error {
		// Call the middleware and the following if it has not been done already
		if !called {
			// Create the next call with the context and the message
			nextWithArgs := func(ctx context.Context) error {
				return next(ctx, msg)
			}

			// Call the middleware and register it as already called
			called = true
			if err := middlewares[0](ctx, msg, nextWithArgs); err != nil {
				return err
			}

			// If next has already been called in middleware, it should not be executed again
			return nextWithArgs(ctx)
		}

		// Nil can be returned, as the next middleware has already been called
		return nil
	}
}

func (c UserController) executeMiddlewares(ctx context.Context, msg *extensions.BrokerMessage, callback extensions.NextMiddleware) error {
	// Wrap middleware to have 'next' function when calling them
	wrapped := c.wrapMiddlewares(c.middlewares, callback)

	// Execute wrapped middlewares
	return wrapped(ctx, msg)
}

func addUserContextValues(ctx context.Context, path string) context.Context {
	ctx = context.WithValue(ctx, extensions.ContextKeyIsVersion, "1.0.0")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsProvider, "user")
	return context.WithValue(ctx, extensions.ContextKeyIsChannel, path)
}

// Close will clean up any existing resources on the controller
func (c *UserController) Close(ctx context.Context) {
	// Unsubscribing remaining channels
	c.UnsubscribeAll(ctx)

	c.logger.Info(ctx, "Closed user controller")
}

// SubscribeAll will subscribe to channels without parameters on which the app is expecting messages.
// For channels with parameters, they should be subscribed independently.
func (c *UserController) SubscribeAll(ctx context.Context, as UserSubscriber) error {
	if as == nil {
		return extensions.ErrNilUserSubscriber
	}

	if err := c.SubscribeCryptellationBacktestsAccountsListResponse(ctx, as.CryptellationBacktestsAccountsListResponse); err != nil {
		return err
	}
	if err := c.SubscribeCryptellationBacktestsAdvanceResponse(ctx, as.CryptellationBacktestsAdvanceResponse); err != nil {
		return err
	}
	if err := c.SubscribeCryptellationBacktestsCreateResponse(ctx, as.CryptellationBacktestsCreateResponse); err != nil {
		return err
	}
	if err := c.SubscribeCryptellationBacktestsOrdersCreateResponse(ctx, as.CryptellationBacktestsOrdersCreateResponse); err != nil {
		return err
	}
	if err := c.SubscribeCryptellationBacktestsOrdersListResponse(ctx, as.CryptellationBacktestsOrdersListResponse); err != nil {
		return err
	}
	if err := c.SubscribeCryptellationBacktestsSubscribeResponse(ctx, as.CryptellationBacktestsSubscribeResponse); err != nil {
		return err
	}

	return nil
}

// UnsubscribeAll will unsubscribe all remaining subscribed channels
func (c *UserController) UnsubscribeAll(ctx context.Context) {
	c.UnsubscribeCryptellationBacktestsAccountsListResponse(ctx)
	c.UnsubscribeCryptellationBacktestsAdvanceResponse(ctx)
	c.UnsubscribeCryptellationBacktestsCreateResponse(ctx)
	c.UnsubscribeCryptellationBacktestsOrdersCreateResponse(ctx)
	c.UnsubscribeCryptellationBacktestsOrdersListResponse(ctx)
	c.UnsubscribeCryptellationBacktestsSubscribeResponse(ctx)
}

// SubscribeCryptellationBacktestsAccountsListResponse will subscribe to new messages from 'cryptellation.backtests.accounts.list.response' channel.
//
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsAccountsListResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsAccountsListResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.accounts.list.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsAccountsListResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsAccountsListResponse will unsubscribe messages from 'cryptellation.backtests.accounts.list.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsAccountsListResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.accounts.list.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsAdvanceResponse will subscribe to new messages from 'cryptellation.backtests.advance.response' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsAdvanceResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsAdvanceResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.advance.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsAdvanceResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsAdvanceResponse will unsubscribe messages from 'cryptellation.backtests.advance.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsAdvanceResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.advance.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsCreateResponse will subscribe to new messages from 'cryptellation.backtests.create.response' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsCreateResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsCreateResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.create.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsCreateResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsCreateResponse will unsubscribe messages from 'cryptellation.backtests.create.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsCreateResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.create.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsEventsID will subscribe to new messages from 'cryptellation.backtests.events.{id}' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsEventsID(ctx context.Context, params CryptellationBacktestsEventsIDParameters, fn func(ctx context.Context, msg BacktestsEventMessage)) error {
	// Get channel path
	path := fmt.Sprintf("cryptellation.backtests.events.%v", params.ID)

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsEventMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsEventsID will unsubscribe messages from 'cryptellation.backtests.events.{id}' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsEventsID(ctx context.Context, params CryptellationBacktestsEventsIDParameters) {
	// Get channel path
	path := fmt.Sprintf("cryptellation.backtests.events.%v", params.ID)

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsOrdersCreateResponse will subscribe to new messages from 'cryptellation.backtests.orders.create.response' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsOrdersCreateResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsOrdersCreateResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.orders.create.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsOrdersCreateResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsOrdersCreateResponse will unsubscribe messages from 'cryptellation.backtests.orders.create.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsOrdersCreateResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.orders.create.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsOrdersListResponse will subscribe to new messages from 'cryptellation.backtests.orders.list.response' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsOrdersListResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsOrdersListResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.orders.list.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsOrdersListResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsOrdersListResponse will unsubscribe messages from 'cryptellation.backtests.orders.list.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsOrdersListResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.orders.list.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
} // SubscribeCryptellationBacktestsSubscribeResponse will subscribe to new messages from 'cryptellation.backtests.subscribe.response' channel.
// Callback function 'fn' will be called each time a new message is received.
func (c *UserController) SubscribeCryptellationBacktestsSubscribeResponse(ctx context.Context, fn func(ctx context.Context, msg BacktestsSubscribeResponseMessage)) error {
	// Get channel path
	path := "cryptellation.backtests.subscribe.response"

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "reception")

	// Check if there is already a subscription
	_, exists := c.subscriptions[path]
	if exists {
		err := fmt.Errorf("%w: %q channel is already subscribed", extensions.ErrAlreadySubscribedChannel, path)
		c.logger.Error(ctx, err.Error())
		return err
	}

	// Subscribe to broker channel
	sub, err := c.broker.Subscribe(ctx, path)
	if err != nil {
		c.logger.Error(ctx, err.Error())
		return err
	}
	c.logger.Info(ctx, "Subscribed to channel")

	// Asynchronously listen to new messages and pass them to app subscriber
	go func() {
		for {
			// Wait for next message
			brokerMsg, open := <-sub.MessagesChannel()

			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then exit the function
			if !open && brokerMsg.IsUninitialized() {
				return
			}

			// Set broker message to context
			ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

			// Execute middlewares before handling the message
			if err := c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
				// Process message
				msg, err := newBacktestsSubscribeResponseMessageFromBrokerMessage(brokerMsg)
				if err != nil {
					return err
				}

				// Add correlation ID to context if it exists
				if id := msg.CorrelationID(); id != "" {
					ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, id)
				}

				// Execute the subscription function
				fn(ctx, msg)

				return nil
			}); err != nil {
				c.logger.Error(ctx, err.Error())
			}
		}
	}()

	// Add the cancel channel to the inside map
	c.subscriptions[path] = sub

	return nil
}

// UnsubscribeCryptellationBacktestsSubscribeResponse will unsubscribe messages from 'cryptellation.backtests.subscribe.response' channel.
// A timeout can be set in context to avoid blocking operation, if needed.
func (c *UserController) UnsubscribeCryptellationBacktestsSubscribeResponse(ctx context.Context) {
	// Get channel path
	path := "cryptellation.backtests.subscribe.response"

	// Check if there subscribers for this channel
	sub, exists := c.subscriptions[path]
	if !exists {
		return
	}

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Stop the subscription
	sub.Cancel(ctx)

	// Remove if from the subscribers
	delete(c.subscriptions, path)

	c.logger.Info(ctx, "Unsubscribed from channel")
}

// PublishCryptellationBacktestsAccountsListRequest will publish messages to 'cryptellation.backtests.accounts.list.request' channel
func (c *UserController) PublishCryptellationBacktestsAccountsListRequest(ctx context.Context, msg BacktestsAccountsListRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.accounts.list.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// PublishCryptellationBacktestsAdvanceRequest will publish messages to 'cryptellation.backtests.advance.request' channel
func (c *UserController) PublishCryptellationBacktestsAdvanceRequest(ctx context.Context, msg BacktestsAdvanceRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.advance.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// PublishCryptellationBacktestsCreateRequest will publish messages to 'cryptellation.backtests.create.request' channel
func (c *UserController) PublishCryptellationBacktestsCreateRequest(ctx context.Context, msg BacktestsCreateRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.create.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// PublishCryptellationBacktestsOrdersCreateRequest will publish messages to 'cryptellation.backtests.orders.create.request' channel
func (c *UserController) PublishCryptellationBacktestsOrdersCreateRequest(ctx context.Context, msg BacktestsOrdersCreateRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.orders.create.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// PublishCryptellationBacktestsOrdersListRequest will publish messages to 'cryptellation.backtests.orders.list.request' channel
func (c *UserController) PublishCryptellationBacktestsOrdersListRequest(ctx context.Context, msg BacktestsOrdersListRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.orders.list.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// PublishCryptellationBacktestsSubscribeRequest will publish messages to 'cryptellation.backtests.subscribe.request' channel
func (c *UserController) PublishCryptellationBacktestsSubscribeRequest(ctx context.Context, msg BacktestsSubscribeRequestMessage) error {
	// Get channel path
	path := "cryptellation.backtests.subscribe.request"

	// Set correlation ID if it does not exist
	if id := msg.CorrelationID(); id == "" {
		msg.SetCorrelationID(uuid.New().String())
	}

	// Set context
	ctx = addUserContextValues(ctx, path)
	ctx = context.WithValue(ctx, extensions.ContextKeyIsDirection, "publication")
	ctx = context.WithValue(ctx, extensions.ContextKeyIsCorrelationID, msg.CorrelationID())

	// Convert to BrokerMessage
	brokerMsg, err := msg.toBrokerMessage()
	if err != nil {
		return err
	}

	// Set broker message to context
	ctx = context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())

	// Publish the message on event-broker through middlewares
	return c.executeMiddlewares(ctx, &brokerMsg, func(ctx context.Context) error {
		return c.broker.Publish(ctx, path, brokerMsg)
	})
}

// WaitForCryptellationBacktestsAccountsListResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsAccountsListResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsAccountsListResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.accounts.list.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsAccountsListResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsAccountsListResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsAccountsListResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsAccountsListResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsAccountsListResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsAccountsListResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsAccountsListResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}

// WaitForCryptellationBacktestsAdvanceResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsAdvanceResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsAdvanceResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.advance.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsAdvanceResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsAdvanceResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsAdvanceResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsAdvanceResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsAdvanceResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsAdvanceResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsAdvanceResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}

// WaitForCryptellationBacktestsCreateResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsCreateResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsCreateResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.create.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsCreateResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsCreateResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsCreateResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsCreateResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsCreateResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsCreateResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsCreateResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}

// WaitForCryptellationBacktestsOrdersCreateResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsOrdersCreateResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsOrdersCreateResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.orders.create.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsOrdersCreateResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsOrdersCreateResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsOrdersCreateResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsOrdersCreateResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsOrdersCreateResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsOrdersCreateResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsOrdersCreateResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}

// WaitForCryptellationBacktestsOrdersListResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsOrdersListResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsOrdersListResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.orders.list.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsOrdersListResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsOrdersListResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsOrdersListResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsOrdersListResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsOrdersListResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsOrdersListResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsOrdersListResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}

// WaitForCryptellationBacktestsSubscribeResponse will wait for a specific message by its correlation ID.
//
// The pub function is the publication function that should be used to send the message.
// It will be called after subscribing to the channel to avoid race condition, and potentially loose the message.
//
// A timeout can be set in context to avoid blocking operation, if needed.
func (cc *UserController) WaitForCryptellationBacktestsSubscribeResponse(ctx context.Context, publishMsg MessageWithCorrelationID, pub func(ctx context.Context) error) (BacktestsSubscribeResponseMessage, error) {
	// Get channel path
	path := "cryptellation.backtests.subscribe.response"

	// Set context
	ctx = addUserContextValues(ctx, path)

	// Subscribe to broker channel
	sub, err := cc.broker.Subscribe(ctx, path)
	if err != nil {
		cc.logger.Error(ctx, err.Error())
		return BacktestsSubscribeResponseMessage{}, err
	}
	cc.logger.Info(ctx, "Subscribed to channel")

	// Close subscriber on leave
	defer func() {
		// Stop the subscription
		sub.Cancel(ctx)

		// Logging unsubscribing
		cc.logger.Info(ctx, "Unsubscribed from channel")
	}()

	// Execute callback for publication
	if err = pub(ctx); err != nil {
		return BacktestsSubscribeResponseMessage{}, err
	}

	// Wait for corresponding response
	for {
		select {
		case brokerMsg, open := <-sub.MessagesChannel():
			// If subscription is closed and there is no more message
			// (i.e. uninitialized message), then the subscription ended before
			// receiving the expected message
			if !open && brokerMsg.IsUninitialized() {
				cc.logger.Error(ctx, "Channel closed before getting message")
				return BacktestsSubscribeResponseMessage{}, extensions.ErrSubscriptionCanceled
			}

			// Get new message
			msg, err := newBacktestsSubscribeResponseMessageFromBrokerMessage(brokerMsg)
			if err != nil {
				cc.logger.Error(ctx, err.Error())
			}

			// If message doesn't have corresponding correlation ID, then continue
			if publishMsg.CorrelationID() != msg.CorrelationID() {
				continue
			}

			// Set context with received values as it is the expected message
			msgCtx := context.WithValue(ctx, extensions.ContextKeyIsBrokerMessage, brokerMsg.String())
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsDirection, "reception")
			msgCtx = context.WithValue(msgCtx, extensions.ContextKeyIsCorrelationID, publishMsg.CorrelationID())

			// Execute middlewares before returning
			if err := cc.executeMiddlewares(msgCtx, &brokerMsg, nil); err != nil {
				return BacktestsSubscribeResponseMessage{}, err
			}

			// Return the message to the caller from the broker that could have
			// been modified by middlewares
			return newBacktestsSubscribeResponseMessageFromBrokerMessage(brokerMsg)
		case <-ctx.Done(): // Set corrsponding error if context is done
			cc.logger.Error(ctx, "Context done before getting message")
			return BacktestsSubscribeResponseMessage{}, extensions.ErrContextCanceled
		}
	}
}
