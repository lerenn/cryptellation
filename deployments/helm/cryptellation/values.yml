config:
  health:
    port: 9000
  nats:
    host:
    port:
  sqldb:
    host:
    port:
    database: candlesticks
credentials:
  exchanges:
    binance:
      api_key:
      secret_key:
  databases:
    sqldb:
      user:
      password:
services:
  candlesticks:
    image: digital-feather/cryptellation:latest
