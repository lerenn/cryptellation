apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: exchanges
  name: exchanges
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exchanges
  strategy: {}
  template:
    metadata:
      labels:
        app: exchanges
    spec:
      containers:
      - name: exchanges
        image: {{ .Values.services.exchanges.image }}
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /liveness
            port: {{ .Values.services.exchanges.health.port | default 9000 }}
        readinessProbe:
          httpGet:
            path: /readiness
            port: {{ .Values.services.exchanges.health.port | default 9000 }}
        command: [ "cryptellation-exchanges", "serve" ]
        env:
        - name: "BINANCE_API_KEY"
          valueFrom:
            configMapKeyRef:
              key: binance.api_key
              name: cryptellation-credentials
        - name: "BINANCE_SECRET_KEY"
          valueFrom:
            configMapKeyRef:
              key: binance.secret_key
              name: cryptellation-credentials
        - name: "HEALTH_PORT"
          valueFrom:
            configMapKeyRef:
              key:  exchanges.health.port
              name: cryptellation-config
        - name: "NATS_HOST"
          valueFrom:
            configMapKeyRef:
              key:  nats.host
              name: cryptellation-credentials
        - name: "NATS_PORT"
          valueFrom:
            configMapKeyRef:
              key:  nats.port
              name: cryptellation-credentials
        - name: "SQLDB_USER"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.user
              name: cryptellation-credentials
        - name: "SQLDB_PASSWORD"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.password
              name: cryptellation-credentials
        - name: "SQLDB_HOST"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.host
              name: cryptellation-credentials
        - name: "SQLDB_PORT"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.port
              name: cryptellation-credentials
        - name: "SQLDB_DATABASE"
          valueFrom:
            configMapKeyRef:
              key:  exchanges.sqldb.database
              name: cryptellation-config
        resources: {}
      initContainers:
      - name: init-exchanges-sqldb
        image: {{ .Values.services.exchanges.image }}
        command: [ "cryptellation-exchanges", "migrations", "migrate" ]
        env:
        - name: "SQLDB_USER"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.user
              name: cryptellation-credentials
        - name: "SQLDB_PASSWORD"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.password
              name: cryptellation-credentials
        - name: "SQLDB_HOST"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.host
              name: cryptellation-credentials
        - name: "SQLDB_PORT"
          valueFrom:
            configMapKeyRef:
              key:  sqldb.port
              name: cryptellation-credentials
        - name: "SQLDB_DATABASE"
          valueFrom:
            configMapKeyRef:
              key:  exchanges.sqldb.database
              name: cryptellation-config
status: {}
