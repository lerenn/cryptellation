apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: candlesticks
  name: candlesticks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: candlesticks
  strategy: {}
  template:
    metadata:
      labels:
        app: candlesticks
    spec:
      containers:
      - name: candlesticks
        image: {{ .Values.services.candlesticks.image }}
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /liveness
            port: {{ .Values.config.health.port | default 9000 }}
        readinessProbe:
          httpGet:
            path: /readiness
            port: {{ .Values.config.health.port | default 9000 }}
        env:
        - name: "BINANCE_API_KEY"
          valueFrom:
            configMapKeyRef:
              key: binance.api_key
              name: cryptellation-credentials
        - name: "BINANCE_SECRET_KEY"
          valueFrom:
            configMapKeyRef:
              key: binance.secret_key
              name: cryptellation-credentials
        - name: "SQLDB_USER"
          valueFrom:
            configMapKeyRef:
              key:  sql.user
              name: cryptellation-credentials
        - name: "SQLDB_PASSWORD"
          valueFrom:
            configMapKeyRef:
              key:  sql.password
              name: cryptellation-credentials
        - name: "SQLDB_HOST"
          valueFrom:
            configMapKeyRef:
              key:  sql.host
              name: cryptellation-config
        - name: "SQLDB_PORT"
          valueFrom:
            configMapKeyRef:
              key:  sql.port
              name: cryptellation-config
        - name: "NATS_HOST"
          valueFrom:
            configMapKeyRef:
              key:  nats.host
              name: cryptellation-config
        - name: "NATS_PORT"
          valueFrom:
            configMapKeyRef:
              key:  nats.port
              name: cryptellation-config
        - name: "HEALTH_PORT"
          valueFrom:
            configMapKeyRef:
              key:  health.port
              name: cryptellation-config
        resources: {}
status: {}
